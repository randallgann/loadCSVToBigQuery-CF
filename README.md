# loadCSVToBigQuery-CF
This repo holds a Google Cloud Function that is triggered by a CSV file being uploaded to a Cloud Storage bucket.  The function takes the CSV file and loads into a BigQuery table.
